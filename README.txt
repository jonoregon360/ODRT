ODRT - OneDrive Renaming Tool
This tool is designed to remove illegal characters from files and folders inside of a OneDrive sync folder, allowing these files to sync properly.

Illegal characters:
 # % * : < > ? / \ \ | { } ~ & ^
Notes:
- & is replaced with 'and', all other characters are replaced with a space
